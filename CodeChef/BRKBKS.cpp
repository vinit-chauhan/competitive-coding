#include <bits/stdc++.h>
using namespace std;
#define debug(x) cerr << #x << " : " << x << endl;

int main() {
    ios::sync_with_stdio(0);
    int test;
    cin >> test;
    while(test--) {
        int power;
        int w[3];

        cin >> power;
        for(int i = 0; i < 3; i++)
            cin >> w[i];

    }
    return 0;
}
