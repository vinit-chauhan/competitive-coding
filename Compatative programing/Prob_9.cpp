#include<iostream>
#include<bits/bits/stdc++.h>
using namespace std;

int main()
{
    int test;
    cin >> test;
    for(int i = 0; i < test; i++)
    {
        int size;
        vector<int> vec;
        cin >> size;

        for(int j = 0; j < size; j++)
        {
            int temp;
            cin >> temp;
            vec.push_back(temp);
        }
        int curmin = vec[0];
        for(j = 1; j < size; j++)
        {
            max = vec[j];
            //

        }
    }
    return 0;
}
